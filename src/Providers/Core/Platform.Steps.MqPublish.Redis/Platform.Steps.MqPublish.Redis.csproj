<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Platform.Steps.MqPublish.Redis</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\..\Sdk\Platform.Sdk\Platform.Sdk.csproj" />
    <ProjectReference Include="..\..\..\Engine\Platform.Engine.Abstractions\Platform.Engine.Abstractions.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- StackExchange.Redis: ConnectionMultiplexer + Streams API (XADD). Compile-time
         MetadataReference only; resolves from the Default ALC and is never loaded into
         the collectible ALC (§5 memory-model). -->
    <PackageReference Include="StackExchange.Redis" />
  </ItemGroup>

</Project>
