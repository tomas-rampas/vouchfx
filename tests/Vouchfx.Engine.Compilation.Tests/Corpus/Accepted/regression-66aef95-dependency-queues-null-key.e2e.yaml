# Regression seed — commit 66aef95. A null-valued 'queues:' key (no value)
# must be accepted and treated identically to no queues being declared.
metadata:
  name: regression-66aef95-dependency-queues-null-key
environment:
  dependencies:
    outbox:
      type: azureservicebus
      queues:
steps:
  - id: noop
    type: script.csharp
    code: "// Filler step: this fixture exercises a null-valued 'queues:' key."
