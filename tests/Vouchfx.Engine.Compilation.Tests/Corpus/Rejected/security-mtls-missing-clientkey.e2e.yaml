# expected-instance-location: /environment/dependencies/events-kafka/security
# expected-keyword: required
#
# REQ-001: 'clientCert' and 'clientKey' are required TOGETHER for profile 'mtls'. A
# security block declaring 'clientCert' but omitting 'clientKey' is rejected here.
metadata:
  name: rejected-security-mtls-missing-clientkey
environment:
  dependencies:
    events-kafka:
      type: kafka
      security:
        profile: mtls
        endpoint: 9093
        clientCert: ./certs/client.pem
steps:
  - id: noop
    type: script.csharp
    code: "// Filler step."
