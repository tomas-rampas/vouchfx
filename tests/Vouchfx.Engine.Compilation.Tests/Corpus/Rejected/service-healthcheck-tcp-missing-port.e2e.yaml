# expected-instance-location: /environment/services/kafka-broker/healthCheck
# expected-keyword: required
#
# services-generalisation spec REQ-009: 'type: tcp' REQUIRES 'port' — a tcp health check
# needs a port to probe. Omitting it is rejected.
metadata:
  name: rejected-service-healthcheck-tcp-missing-port
environment:
  services:
    kafka-broker:
      image: myorg/kafka-broker:1.0
      ports: [9093]
      healthCheck:
        type: tcp
steps:
  - id: noop
    type: script.csharp
    code: "// Filler step."
