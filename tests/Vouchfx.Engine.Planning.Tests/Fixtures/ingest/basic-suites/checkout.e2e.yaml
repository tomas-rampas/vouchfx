metadata:
  name: checkout-flow
  owner: qa-team
  tags: [smoke]

environment:
  services:
    api:
      image: myorg/checkout-api:1.0
  dependencies:
    orders-db:
      type: postgres
    events:
      type: kafka

steps:
  - id: create-order
    type: http.rest
    target: api
  - id: assert-order-row
    type: db-assert.postgres
    target: orders-db
